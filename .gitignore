__pycache__/
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# Generated by `python -m tools.seed` — source data, the local catalog, and
# anything a run writes. All reproducible from a clean clone, so none of it
# belongs in git.
example/warehouse/
example/catalog.duckdb*
out/

# Extracted from the published post by tools/extract_from_post.py. Gitignored
# on purpose: a committed copy would be a second source of truth, free to drift
# from the article it claims to mirror.
tests/fixtures/published/
